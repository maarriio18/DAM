#include <stdio.h>
#include <stdlib.h>
/**
 * 
 * 		Autor: mario perez
 */
int main(){
	int condicion;
	char c;
	scanf("%d, &condicion")
	c = (condicion > 5) ? 's' : 'n' 
				

	return EXIT_SUCCESS;
}
